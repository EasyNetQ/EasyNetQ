assembly-versioning-scheme: MajorMinorPatch
next-version: 2.0.4
mode: ContinuousDelivery
branches:
  master:
    mode: ContinuousDelivery
  features?[/-]:
    mode: ContinuousDeployment
ignore:
  sha: []