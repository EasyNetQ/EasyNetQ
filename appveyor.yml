version: 1.0.{build}

build:
  project: Source\EasyNetQ.sln
  parallel: true
  verbosity: minimal
  
configuration: Release
platform: Any CPU

branches:
  only:
  - master

artifacts:
  - path: Package\**\*.nupkg
    name: EasyNetQ
